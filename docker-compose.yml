version: '3'
services:
  raspberry-status-server:
    build: .
    image: stijink/raspberry-status-server
    working_dir: /var/www/
    command: nodemon index.js
    ports:
      - "9360:9360"
    volumes:
      - .:/var/www/
